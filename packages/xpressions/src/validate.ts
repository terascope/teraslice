import { Options } from './interfaces';

/**
 * Validate an expression string and ensure the variable will exists
 *
 * @returns a list of errors
*/
export function validate(_input: string, _options: Options): string[] {
    return [];
}
