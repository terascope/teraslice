import { Options } from './interfaces';

/**
 * Evaluate all of the templated variables in a xpression string
 *
 * @returns the input with the translated values
*/
export function transform(input: string, _options: Options): string {
    return input;
}
