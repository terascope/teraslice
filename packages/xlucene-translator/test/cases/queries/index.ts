import type { TestCaseCollection } from './interfaces.js';
import { arrays, empty_arrays } from './arrays.js';

const tests: TestCaseCollection = {
    arrays: arrays,
    empty_arrays,
};

export default tests;
