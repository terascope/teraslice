'use strict';

const TestContext = require('./test-context');
const {
    newConfig, newSysConfig, opsPath, newId
} = require('./configs');

module.exports = {
    newConfig,
    opsPath,
    newId,
    newSysConfig,
    TestContext
};
