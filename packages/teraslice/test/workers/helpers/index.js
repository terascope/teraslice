'use strict';

const TestContext = require('./test-context');
const {
    newSliceConfig,
    newConfig,
    newSysConfig,
    opsPath,
    newId,
} = require('./configs');


module.exports = {
    newConfig,
    newSliceConfig,
    opsPath,
    newId,
    newSysConfig,
    TestContext,
};
