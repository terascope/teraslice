{
  "kind": "Service",
  "apiVersion": "v1",
  "metadata": {
    "labels": {
        "app": "teraslice",
        "nodeType": "{{nodeType}}",
        "exId": "{{exId}}",
        "jobId": "{{jobId}}"
    },
    "name": "{{name}}",
    "namespace": "{{namespace}}"
  },
  "spec": {
    "ports": [
      {
        "port": 45680,
        "targetPort": 45680
      }
    ],
    "selector": {
      "app": "teraslice",
      "nodeType": "{{nodeType}}",
      "exId": "{{exId}}"
    }
  }
}