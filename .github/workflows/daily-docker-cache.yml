name: Daily Docker Cache

on:
  schedule:
    - cron: '0 12 * * 1-5' # Monday - Friday at 5am Arizona Time
  workflow_dispatch:

jobs:
  call-refresh-docker-cache-workflow:
    uses: terascope/workflows/.github/workflows/refresh-docker-cache.yml@58e2d0781a89034f271a1d2a5122ac6b2c3a288f
    secrets: inherit
