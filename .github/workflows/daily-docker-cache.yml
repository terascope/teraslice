name: Daily Docker Cache

on:
  schedule:
    - cron: '0 12 * * 1-5' # Monday - Friday at 5am Arizona Time
  workflow_dispatch:

jobs:
  call-refresh-docker-cache-workflow:
    uses: terascope/workflows/.github/workflows/refresh-docker-cache.yml@f52b2a36f69ae044270d12fff5ccf4a269a48cc5
    secrets: inherit
