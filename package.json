{
    "name": "teraslice-workspace",
    "private": true,
    "scripts": {
        "start": "node service.js",
        "setup": "lerna bootstrap --force-local && yarn build:cleanup dist && yarn build",
        "cleanup": "./scripts/cleanup.sh",
        "prebuild": "lerna run prebuild",
        "build": "tsc --build --pretty",
        "build:cleanup": "./scripts/build-cleanup.sh",
        "build:all": "yarn build:cleanup && env FORCE_COLOR=1 lerna run build",
        "build:pkg": "./scripts/build-pkg.sh",
        "build:watch": "yarn run build --watch",
        "benchmark": "lerna run --concurrency=1 --no-prefix --stream benchmark",
        "sync": "ts-scripts sync",
        "docs": "ts-scripts docs",
        "bump": "ts-scripts bump",
        "test": "ts-scripts test",
        "lint": "eslint --cache --ext .js,.jsx,.ts,.tsx . && yarn sync --verify",
        "lint:fix": "eslint --cache --ext .js,.jsx,.ts,.tsx --fix . && yarn sync"
    },
    "displayName": "Teraslice",
    "homepage": "https://github.com/terascope/teraslice",
    "documentation": "https://terascope.github.io/teraslice",
    "dependencies": {},
    "bugs": {
        "url": "https://github.com/terascope/teraslice/issues"
    },
    "devDependencies": {
        "@types/bluebird": "^3.5.27",
        "@types/convict": "^4.2.1",
        "@types/elasticsearch": "^5.0.34",
        "@types/fs-extra": "^8.0.0",
        "@types/jest": "^24.0.18",
        "@types/lodash": "^4.14.137",
        "@types/lodash.clonedeep": "^4.5.6",
        "@types/nanoid": "^2.0.0",
        "@types/node": "^12.7.2",
        "@types/uuid": "^3.4.4",
        "jest": "^24.9.0",
        "jest-extended": "^0.11.2",
        "jest-watch-typeahead": "^0.4.0",
        "lerna": "^3.16.4",
        "lerna-alias": "^3.0.2",
        "ts-jest": "^24.0.2",
        "typescript": "^3.5.3"
    },
    "workspaces": [
        "packages/*",
        "e2e"
    ],
    "terascope": {
        "root": true,
        "type": "monorepo",
        "docker": {
            "registry": "terascope/teraslice",
            "cache_layers": [
                {
                    "from": "node:10.16.3-alpine",
                    "name": "base"
                },
                {
                    "from": "base",
                    "name": "connectors"
                }
            ]
        }
    },
    "engines": {
        "node": ">=8.16.0",
        "yarn": ">=1.16.0"
    },
    "os": [
        "darwin",
        "linux"
    ]
}
